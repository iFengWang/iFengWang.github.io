services:
  blog:
    build: .
    command: bundle exec jekyll serve --host 0.0.0.0 --port 4000 --force_polling
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
      - bundle_cache:/usr/local/bundle

volumes:
  bundle_cache:
